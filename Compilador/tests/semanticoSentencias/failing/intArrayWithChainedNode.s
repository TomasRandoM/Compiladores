// Incorrecto. Se llama al método concat desde un objeto del array que no es Str
class A {
    pub Array Int x;
    Int y;
}

impl A {
    .() {
    }
    fn a(Int a) {
        y = 5;
    }
}

start {
    Int x;
    A a;
    x = 4;
    (a.a(5));
    a.x = new Int[5];
    (IO.out_int(a.x[0].length()));
}
