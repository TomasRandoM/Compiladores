// Correcto. Se llama a un metodo con parámetros de igual tipo
class A {
    Int x;
    Int y;
}

impl A {
    .() {
    }
    fn a(Double a) {
        y = 5;
        (a(5.5));
    }
}

start {
    Int x;
    A a;
    x = 4;
}
