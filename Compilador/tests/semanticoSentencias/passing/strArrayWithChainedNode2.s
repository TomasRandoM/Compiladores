// Correcto. Se llama al método length desde un objeto del array que es Str
class A {
    pub Array Str x;
    Int y;
}

impl A {
    .() {
    }
    fn a(Int a) {
        y = 5;
    }
}

start {
    Int x;
    A a;
    a = new A();
    a.x = new Str[5];

    (IO.out_int(a.x[0].length()));
}
